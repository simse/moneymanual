---
import ArticlePage from "@components/page/ArticlePage.astro";
import { getPageEntry } from "../lib/content";

const { slug } = Astro.params;
const page = await getPageEntry("en-gb", slug);

if (!page) {
	return Astro.rewrite("/404");
}
---
<ArticlePage locale="en-gb" page={page} />
