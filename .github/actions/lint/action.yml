name: Lint
description: Run biome lint after installing the toolchain and dependencies.

runs:
  using: composite
  steps:
    - uses: ./.github/actions/setup

    - name: Check formatting
      shell: bash
      run: pnpm run fmt:check

    - name: Lint
      shell: bash
      run: pnpm run lint
