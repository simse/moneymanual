name: Astro check
description: Run astro check (type-checks .astro files) after installing the toolchain and dependencies.

runs:
  using: composite
  steps:
    - uses: ./.github/actions/setup

    - name: Astro check
      shell: bash
      run: pnpm run astro check
