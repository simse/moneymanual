name: Setup toolchain
description: Install pnpm and node via setup-toolchain, then run pnpm install with the locked dependencies.

runs:
  using: composite
  steps:
    - uses: moonrepo/setup-toolchain@v0
      with:
        auto-install: true

    - name: Install dependencies
      shell: bash
      run: pnpm install --frozen-lockfile
